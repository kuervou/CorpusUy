---
import MemberCard from "../components/MemberCard.astro";
---
  
  <!-- Sección de Integrantes -->
  <section class="w-full min-h-screen bg-gray-900 text-white pt-8 pb-16">
    <h1 class="text-center text-4xl mb-8 font-bold">Integrantes</h1>

    <!-- Tabaré Calderón -->
    <MemberCard
      nombre="Tabaré Calderón"
      rol="Voz"
      imageVertical="/integrants/tabaV.webp"
      imageHorizontal="/integrants/tabaH.webp"
      stats={`
        Nuestro frontman. Sangre nueva en el proyecto, ha venido a dejar huella.
      `}
    />

    <!-- Willy -->
    <MemberCard
      nombre="Wilson Martínez"
      rol="Guitarra"
      imageVertical="/integrants/willV.webp"
      imageHorizontal="/integrants/willH.webp"
      stats={`
        En el escenario es el amo y señor de las 6 cuerdas, en el estudio es el escultor que le da forma a nuestro sonido. 
      `}
    />

    <!-- Cuervo -->
    <MemberCard
      nombre="Hernán Cabara"
      rol="Bajo"
      imageVertical="/integrants/cuervoV.webp"
      imageHorizontal="/integrants/cuervoH.webp"
      stats={`
        El cuervo. Cuenta la leyenda que cuando sube a tocar, 
        es poseído por el Dios Corpus.
      `}
    />

    <!-- Ludwig -->
    <MemberCard
      nombre="Ludwig Van Velthoven"
      rol="Batería"
      imageVertical="/integrants/ludV.webp"
      imageHorizontal="/integrants/ludH.webp"
      stats={`
        Este energético ser humano trae folklore en la sangre, rock en la espalda y un pulso que no necesita presentación. 
      `}
    />
  </section>

  <script is:inline>
    if (typeof window !== 'undefined') {
      const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
          const target = entry.target;
          if (entry.isIntersecting) {
            target.classList.remove('grayscale');
          } else {
            target.classList.add('grayscale');
          }
        });
      }, {
        threshold: 0.6
      });
  
      document.querySelectorAll('.observer-target').forEach((el) => {
        observer.observe(el);
      });
    }
  </script>
  