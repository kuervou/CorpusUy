---
import MemberCard from "../components/MemberCard.astro";
---

<!-- Header de la sección -->
<section class="w-full bg-dark text-white py-20 px-4">
  <div class="max-w-4xl mx-auto text-center space-y-3">
    <p class="text-xs tracking-[0.35em] uppercase text-gray-400">
      La columna vertebral de Corpus
    </p>
    <h1 class="text-4xl font-bold">Integrantes</h1>
    <p class="text-sm text-accent max-w-xl mx-auto">
      Juntos transformamos cada show en un ritual.
    </p>
  </div>
</section>

<!-- Cards con imagen full-bleed -->
<section class="w-full bg-gray-900 text-white">
  <div class="flex flex-col">

    <MemberCard
      nombre="Tabaré Calderón"
      rol="Voz"
      imageVertical="/integrants/tabaV.webp"
      imageHorizontal="/integrants/tabaH.webp"
      stats={`Nuestro frontman. Sangre nueva en el proyecto, ha venido a dejar huella.`}
    />

    <MemberCard
      nombre="Wilson Martínez"
      rol="Guitarra"
      imageVertical="/integrants/willV.webp"
      imageHorizontal="/integrants/willH.webp"
      stats={`En el escenario es el amo y señor de las 6 cuerdas, en el estudio es el escultor que le da forma a nuestro sonido.`}
      posicion="abajo"
    />

    <MemberCard
      nombre="Hernán Cabara"
      rol="Bajo"
      imageVertical="/integrants/cuervoV.webp"
      imageHorizontal="/integrants/cuervoH.webp"
      stats={`El cuervo. Cuenta la leyenda que cuando sube a tocar, es poseído por el Dios Corpus.`}
    />

    <MemberCard
      nombre="Ludwig Van Velthoven"
      rol="Batería"
      imageVertical="/integrants/ludV.webp"
      imageHorizontal="/integrants/ludH.webp"
      stats={`Este energético ser humano trae folklore en la sangre, rock en la espalda y un pulso que no necesita presentación.`}
    />

  </div>

  <script is:inline>
    if (typeof window !== 'undefined') {
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            const target = entry.target;
  
            if (entry.isIntersecting) {
              target.classList.remove('grayscale');
              target.classList.add('scale-105');
            } else {
              target.classList.add('grayscale');
              target.classList.remove('scale-105');
            }
          });
        },
        {
          threshold: 0.6,
        }
      );
  
      document.querySelectorAll('.observer-target').forEach((el) => {
        observer.observe(el);
      });
    }
  </script>
  
  
</section>
